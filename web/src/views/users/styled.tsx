import { Box, styled } from "@mui/material";

export const StyledBox = styled(Box)(() => ({
  display: "flex",
  flexDirection: "row",
  justifyContent: "left",
  alignItems: "left",
  width: "100%",
}));
